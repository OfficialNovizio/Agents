---
name: atlas-logical-book-requirements
type: logical (placeholder — no book provided yet)
status: awaiting source material; nothing in this folder should be treated as a built logical skill until a real book/source is supplied
assigned_agent: atlas (Brand Studio / Art Director)
date_added: 2026-07-07
---

## Purpose

Records what kind of real, named source would ground atlas's logical layer. None provided yet.

## Why Atlas Specifically Needs This

Atlas's craft rules (Gestalt, hierarchy, grids, the WCAG math) are well-sourced practitioner content, but two judgments currently run on rubric/heuristic where formal grounding exists: **color perception** (the multi-brand "near-miss" flag has no perceptual distance measure behind it — "perceptually close" is currently eyeballed) and **visual attention** (why hierarchy and focal-point rules work — currently asserted, not grounded). Per rule 0.6, near-miss color findings are flagged as judgment-based until grounded.

## What Kind of Book Would Actually Help

1. **Color science / perceptual color spaces** — a source covering CIELAB/ΔE color-difference math. Directly upgrades multi-brand-system's near-miss detection ("ΔE < [threshold] to a sibling's token" is computable) and brand-identity's palette work. This is the most formula-shaped gap atlas has.
2. **Visual perception and attention** (e.g., a rigorous text on visual perception in design) — grounds layout-composition's hierarchy/focal-point rules and connects to the Behavioral Science department's attention work (kahneman) — coordinate so the two departments cite compatible sources.

## What NOT to Use

No general knowledge, no taste anthologies without testable content, nothing that restates the Gestalt/grid material already in layout-composition.

## Instructions for When a Book Is Provided

1. Confirm title, edition, relevant chapters. 2. Extract one formula/framework at a time (rule 0.2). 3. Build each as a logical skill citing chapter/section. 4. Cross-reference which skill it extends (e.g., "this ΔE threshold computes multi-brand-system's near-miss flag"). 5. Update this file's `status`.

## Current Status

No book provided. No logical skills built. This file is the only content in atlas's `logical/` folder.
