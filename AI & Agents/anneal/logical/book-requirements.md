# anneal — Logical Layer (placeholder)

No logical skills yet — lifecycle/audit judgments are **reasoning-based, not formula-verified** (rule 0.6 flag active).

**What's needed:** a source on document/knowledge-base quality measurement or software maintenance economics (e.g. technical-debt quantification literature) — operator supplies.

**What it would ground:** numeric staleness scoring (instead of a fixed 12-month flag), edit-impact estimation (which skills' failures cost most → audit priority ordering), lesson-to-edit conversion-rate targets.

**How to fill:** operator supplies source → extract formulas with citations → replace fixed cadences/flags with computed priorities → un-flag.
