# gauge — Logical Layer (placeholder)

No logical skills yet — thresholds and drift windows are **reasoning-based, not formula-verified** (rule 0.6 flag active).

**What's needed:** a statistics source (statistical process control — control charts, drift detection; e.g. an SPC text or an applied stats book the operator supplies). This is the SAME shared-statistics want named by vista/sentinel/nate/kai/rio (PROJECT-HANDOFF §7) — one book could ground all of them; coordinate at acquisition.

**What it would ground:** control limits instead of fixed ±20% drift; distinguishing noise from signal in small task counts; golden-set sample sizes.

**How to fill:** operator supplies the book → extract formulas with citations → replace scorecard.py's fixed thresholds with computed control limits → un-flag.
