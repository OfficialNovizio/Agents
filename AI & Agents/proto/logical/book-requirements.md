# proto — Logical Layer (placeholder)

No logical skills yet — timeboxes, criteria counts, and lifetime defaults are **reasoning-based, not formula-verified** (rule 0.6 flag active).

**What's needed:** an experimentation-methodology source (e.g. lean-experimentation / design-of-experiments literature; the catalog's Product dept lists lean-experimentation — coordinate to avoid duplicate acquisition).

**What it would ground:** prototype lifetime as a function of hypothesis class; minimum-evidence thresholds for PROMOTE; portfolio math (how many concurrent prototypes the fleet can cage safely).

**How to fill:** operator supplies source → extract formulas with citations → verdict bars become computed → un-flag.
