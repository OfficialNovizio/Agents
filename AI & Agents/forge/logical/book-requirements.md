# forge — Logical Layer (placeholder)

No logical skills yet — replication counts, watch windows, and frontier reads are **reasoning-based, not formula-verified** (rule 0.6 flag active).

**What's needed:** an experimental-design/statistics source (power analysis, multiple-comparison correction, A/B methodology — the shared statistics book named by gauge/vista/sentinel/nate/kai/rio would serve; coordinate one acquisition).

**What it would ground:** replication counts from power analysis instead of "suggested 5"; significance thresholds for "no measured difference"; cost-quality frontier confidence bands.

**How to fill:** operator supplies the book → extract formulas with citations → benchmarking-discipline gains a real stats core → un-flag.
