f=Cybersecurity/bastion/operational/agent/bastion-config.md · h1:bastion Config (fill-in template; field traces skill line)=\| Field \| Referenced by \| Value \| · h1:bastion Config (fill-in template; field traces skill line)=\|---\|---\|---\| · h1:bastion Config (fill-in template; field traces skill line)=\| cloud_accounts (providers, accounts) \| cloud-posture \| `<FILL_IN — stack-profile hosting config>` \| · h1:bastion Config (fill-in template; field traces skill line)=\| cspm_tool \| cloud-posture \| `<FILL_IN — Prowler / ScoutSuite / Security Hub / Defender>` \| · h1:bastion Config (fill-in template; field traces skill line)=\| cspm_scan_cadence \| cloud-posture \| `<FILL_IN — default monthly>` \| · h1:bastion Config (fill-in template; field traces skill line)=\| cve_feeds \| infra-vuln-management \| `<FILL_IN — NVD / OSV / vendor advisories>` \| · h1:bastion Config (fill-in template; field traces skill line)=\| patch_sla_critical / patch_sla_high / patch_sla_medium \| infra-vuln-management \| `<FILL_IN — default 7/30/90 days>` \| · h1:bastion Config (fill-in template; field traces skill line)=\| vuln_scan_cadence \| infra-vuln-management \| `<FILL_IN — default weekly OS / monthly cloud>` \| · h1:bastion Config (fill-in template; field traces skill line)=\| network_scan_tool \| network-security \| `<FILL_IN>` \| · h1:bastion Config (fill-in template; field traces skill line)=\| network_review_cadence \| network-security \| `<FILL_IN — default quarterly>` \| · h1:bastion Config (fill-in template; field traces skill line)=\| executor \| all (the inversion) \| operator / ops (bastion holds no keys) \| · in=`cloud_accounts` cloud posture checks impossible; flag risk warden. Focus local infra hardening network review. `patch_sla_*` values default 7/30/90 days, loudly labeled provisional; recommend operator confirmation. `executor` fixed operator/ops config grants bastion key-holding execution (the security-inversion). · f=Unfilled config degrades loudly. absolute: bastion never holds cloud admin keys, pushes firewall changes, applies patches itself operator/ops executes infra changes.