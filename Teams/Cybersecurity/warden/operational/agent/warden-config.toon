f=Cybersecurity/warden/operational/agent/warden-config.md · h1:warden Config (fill-in template; field traces skill line)=\| Field \| Referenced by \| Value \| · h1:warden Config (fill-in template; field traces skill line)=\|---\|---\|---\| · h1:warden Config (fill-in template; field traces skill line)=\| control_standard \| security-policy-framework \| `<FILL_IN: NIST CSF / ISO 27001 / SOC 2 / CIS — installs the GRC pack>` \| · h1:warden Config (fill-in template; field traces skill line)=\| grc_pack_installed \| security-policy-framework \| `<FILL_IN — Sushegaad GRC pack for the chosen standard; runtime-install>` \| · h1:warden Config (fill-in template; field traces skill line)=\| security_profile (crown-jewel assets, obligations, cloud accounts) \| risk-register / all \| `<FILL_IN per business>` \| · h1:warden Config (fill-in template; field traces skill line)=\| likelihood_scale / impact_scale \| risk-register / risk_score.py \| `<FILL_IN — default 5×5; reasoning-based (0.6)>` \| · h1:warden Config (fill-in template; field traces skill line)=\| risk_acceptance_threshold \| risk-register / security-exception-process \| `<FILL_IN — above → board; e.g. score ≥ 15>` \| · h1:warden Config (fill-in template; field traces skill line)=\| risk_register_path / exception_register_path \| risk-register / exceptions \| `<FILL_IN — append-only>` \| · h1:warden Config (fill-in template; field traces skill line)=\| vendor_review_cadence \| third-party-risk \| `<FILL_IN>` \| · h1:warden Config (fill-in template; field traces skill line)=\| board_route \| risk-register / exceptions / policy changes \| board (Governance) — risk acceptance + Rail 3 changes \| · h1:warden Config (fill-in template; field traces skill line)=\| privileged_executor \| all (the inversion) \| operator (warden holds no keys) \| · in=`control_standard` default CIS Controls IG1 baseline, provisional; surface GRC pack adoption. scoring scales default 5×5, loudly flagged reasoning-based; propose operator. `privileged_executor` fixed operator config grants warden key-holding execution (the security-inversion). Risk acceptance above `risk_acceptance_threshold` material policy changes route board; unfilled board route (Governance dormant) decisions QUEUE, proceed (most-restrictive). · f=Unfilled config degrades loudly. absolute: warden never holds keys accepts material risk itself operator's/board's, design.