f=Cybersecurity/keyring/operational/agent/keyring-config.md · h1:keyring Config (fill-in template; field traces skill line)=\| Field \| Referenced by \| Value \| · h1:keyring Config (fill-in template; field traces skill line)=\|---\|---\|---\| · h1:keyring Config (fill-in template; field traces skill line)=\| identity_provider (IdP/SSO) \| identity-lifecycle / access-reviews \| `<FILL_IN — the IdP; executes grants/revokes>` \| · h1:keyring Config (fill-in template; field traces skill line)=\| role_access_baselines_path \| identity-lifecycle / access-reviews / access_review.py \| `<FILL_IN — role → least-privilege entitlement sets>` \| · h1:keyring Config (fill-in template; field traces skill line)=\| access_review_cadence \| access-reviews \| `<FILL_IN — suggested quarterly, mirrors Fleet Charter Rail 2>` \| · h1:keyring Config (fill-in template; field traces skill line)=\| privileged_review_cadence \| privileged-access-management \| `<FILL_IN — shorter than normal>` \| · h1:keyring Config (fill-in template; field traces skill line)=\| pam_tool \| privileged-access-management \| `<FILL_IN — just-in-time / break-glass executor>` \| · h1:keyring Config (fill-in template; field traces skill line)=\| secrets_manager \| secrets-governance \| `<FILL_IN — the vault; holds + rotates>` \| · h1:keyring Config (fill-in template; field traces skill line)=\| secret_rotation_cadence \| secrets-governance \| `<FILL_IN>` \| · h1:keyring Config (fill-in template; field traces skill line)=\| deprovision_sla \| identity-lifecycle \| `<FILL_IN — leaver time-to-deprovision target>` \| · h1:keyring Config (fill-in template; field traces skill line)=\| executor \| all (the inversion) \| operator / IdP / PAM / vault (keyring holds no keys) \| · h1:keyring Config (fill-in template; field traces skill line)=\| hr_signal_source \| identity-lifecycle \| `<FILL_IN — future People & Culture / HR joiner-leaver feed>` \| · in=IdP manual access matrix, reconciled cadence (identity-lifecycle fallback), labeled manual. `role_access_baselines_path` access-reviews builds provisional baselines current-minus-excess; sharpens over cycles. `secrets_manager` top register risk (plaintext secrets); interim encrypted store, labeled provisional. `executor` fixed operator/IdP/PAM/vault config grants keyring key-holding execution (the security-inversion). · f=Unfilled config degrades loudly. absolute: keyring never holds keys executes grant/revoke/rotation operator/IdP/PAM/vault does.