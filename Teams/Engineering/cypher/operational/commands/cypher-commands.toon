f=Engineering/cypher/operational/commands/cypher-commands.md · p=Routing reference cypher. overriding rule: caged-scope runs before any skill, invocation. · h2:Trigger Table=\| Skill \| Natural-language triggers \| Shortcut \| · h2:Trigger Table=\|---\|---\|---\| · h2:Trigger Table=\| caged-scope \| (automatic, first, always) "is this in scope," "can I attack X" \| `/cypher-scope` \| · h2:Trigger Table=\| attack-playbooks \| "attack," "red team," "test prompt injection," "hijack test," "tool poisoning" \| `/cypher-attack` \| · h2:Trigger Table=\| continuous-attack-loop \| "run the loop," "continuous attack," "posture," "re-attack patches" \| `/cypher-loop` \| · h2:Trigger Table=\| findings-report \| "report findings," "file the breach" \| `/cypher-report` \| · h3:caged-scope precedes everything=attack, loop, report skill executes until caged-scope passes action. routing preference it's safety property. request "just quickly attack still passes gate first. · h3:"attack" single run standing loop=specific one-off attack **attack-playbooks**., Ongoing/scheduled/posture **continuous-attack-loop** (which calls attack-playbooks). · h3:Findings go one way=Any breach **findings-report** quinn intake only. Never directly builder, never "cypher fixes it" (offense doesn't fix), never stored exploit. · h3:What cypher never does=Act without signed scope (does nothing)., Attack third-party production customer systems (out cage)., Leave live changes, persistence, weaponizable artifact., Route around quinn, amend scope (operator-only). · f=signed scope cypher nothing says (caged-scope). Any ambiguity about scope, target ownership, sandbox-containment fails closed. request weaponize attack outside cage refused escalated operator.