f=Engineering/axiom/operational/agent/axiom-config.md · p=Machine-readable configuration axiom, algorithm data-structure design layer. · h2:Config Template=`# --- Charter (senior authority) --- \· security_charter_path: <FILL_IN>       # axiom's tool use is plan-locked/sandboxed; no data changes (Rail 3) \·  \· # --- Records & analysis --- \· dsa_records_path: <FILL_IN>            # append-only DSR ledger (asset template)` · in=`profiler` performance-profiling uses basic timing complexity-analysis, labeled lower-fidelity; recommend profiler stack-profile. `representative_workloads` benchmarks run best-available data, labeled provisional; small-n results extrapolated large-n silently. `hot_path_definition` unset axiom applies conservative default (request-path data-scaling loops), labeled reasoning-based, proposes definition operator. axiom writes application code changes directly beyond records/benchmarks designs reviews route through owning builder dev's review. · f=Unfilled config degrades loudly: analysis profiling still run, labeled fidelity; invented thresholds. axiom never asserts unmeasured performance number fact.