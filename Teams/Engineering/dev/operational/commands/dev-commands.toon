f=Engineering/dev/operational/commands/dev-commands.md · p=Routing reference for dev. · h2:Trigger Table=\| Skill \| Natural-language triggers \| Shortcut \| · h2:Trigger Table=\|---\|---\|---\| · h2:Trigger Table=\| architecture-decisions \| "architecture decision," "should we use X," "why did we choose Y," "record an ADR" \| `/dev-adr` \| · h2:Trigger Table=\| stack-profile \| "stack profile," "what are we built with," "what framework/db/host," "set up our tech doc" \| `/dev-stack` \| · h2:Trigger Table=\| code-review-standards \| "review this PR," "code review," "is this mergeable" (code quality) \| `/dev-review` \| · h2:Trigger Table=\| delivery-governance \| "is this done," "can we merge" (process), "log tech debt," "branching model" \| `/dev-done` \| · h3:Decision vs documentation=Making technical choice architecture-decisions (ADR). Recording current state stack-profile. stack change both: ADR first, profile update. · h3:Review (code) done-check (process)=`/dev-review` code itself (correctness/security/tests/style). `/dev-done` whole definition done (review quinn aegis ops charter docs). passes review isn't "done" until gates requires green. · h3:Charter is senior=Any request weaken Security Charter rail refused routed operator amendment question never handled ADR config change. · h3:What dev take=Actual data changes dana authors scripts, operator runs them (Rail 3)., release gate quinn. Deploys ops. Deep security aegis. Domain implementation raj/mia/nova/dana/axiom. · f=Ambiguous decide (ADR), document (stack), review (code), done-check (process)?