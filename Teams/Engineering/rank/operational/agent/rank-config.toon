f=Engineering/rank/operational/agent/rank-config.md · p=Machine-readable configuration rank, technical-SEO executor. · h2:Config Template=`# --- Charter (senior authority) --- \· security_charter_path: <FILL_IN>       # plugin runs plan-locked/sandboxed; rank runs no data changes (Rail 3); no direct production edits \·  \· # --- The plugin --- \· claude_seo_installed: <FILL_IN>        # AgriciDaniel/claude-seo (MIT) — runtime-installed connector (plan §5)` · in=`security_charter_path` most-restrictive: analysis-only, plugin execution against live systems; stated outputs. `claude_seo_installed` false rank works method-only (technical-seo-execution structured-data-geo), labeled reduced-automation; surface plugin connector operator (plan §5). `suppress_community_footer: true` (fixed default) plugin's promo footer stripped operator-facing business output. Ownership fixed plan's split: strategy/measurement kai, technical execution rank; task wrong side handed over brief, never absorbed. rank specs fixes; `frontend_impl`/`server_impl` implement through `review`/`gate` rank never auto-edits production plugin. · f=Unfilled config degrades loudly. Without plugin, rank's method carries technical SEO; kai boundary no-direct-edit rule hold regardless.