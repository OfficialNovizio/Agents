f=Engineering/ops/custom/maintenance-hygiene/SKILL.md · in=maintenance-hygiene standing care keeps healthy system healthy: dependency patch cadence, **restore-tested backups** backup never restored rumor), monitoring baselines (the "normal" deploy incident judged against), expiry register (certs, tokens, domains, quotas things break schedule nobody watches). anti-entropy skill: unglamorous, scheduled, reason there's less incident-response do. · p=Systems don't break changed they rot place. Dependencies accrue CVEs, backups silently stop working, certs expire Sunday, "normal" drifts until nobody knows what healthy looks like. one fully preventable incident. Hygiene converts them surprises calendar entries. · wu=Triggers: "maintenance," "update dependencies," "are backups working," "what's our baseline," "cert expiry," "patch CVE," scheduled cadence runs (config), post-incident baseline updates, quinn/dev asking "what normal look like" during verification. · st:/ Protocol=`THE REGISTER (assets/maintenance-register-template.md — one document, four sections) \·  \· 1 DEPENDENCIES & PATCHES \· cadence per config → inventory drift vs stack-profile → security patches by CVE severity \· → every update ships through the NORMAL pipeline: quinn's gate + release-discipline` · in=**Work cadence, mood.** section runs configured schedule (`maintenance_cadences`); skipped run logged skipped silent skips rot compounds. Findings dated entries register. **Dependency updates deploys.** Patch-level security updates ride hotfix row CVE demands speed; everything else batches cadence passes quinn's gate release-discipline's checklist. "it's dep bump" bypass; lockfile drift stack-profile finding (dev's rule). **A backup proven restore.** cadence: restore backup scratch environment, verify integrity (counts, checksums, app boots against it), record evidence measured restore time (that number real recovery floor during incidents). failed restore test incident, register note. **Baselines dated facts.** "Normal" measured numbers measurement date, folklore. Deploy verification (release-discipline) incident "resolved" reference them, stale baselines corrupt two skills re-baseline after material changes cadence. **Nothing expires unwatched.** cert, token, key, domain, quota lives expiry register owner alert lead time. Sunday-night cert expiry preventable incident class exists. **Propose, don't invent, thresholds.** Alert thresholds, retention windows, cadences operator-adopted config (rule 0.5). ops recommends values reasoning (labeled reasoning-based rule 0.6 until logical layer grounds them) never silently enforces guesses. · of=`## Hygiene Run: [section] — [date] (cadence: [met/late]) \· Findings: [dated entries — CVE/drift/failed-restore/stale-baseline/upcoming-expiry] \· Actions: [batched update → gate/deploy refs \· restore evidence + duration \· re-baseline + date \· renewals scheduled] \· Escalations: [failed restore test → P2 \· unpatched critical CVE past window → operator]` · pr=**Restore-tested nonexistent** backup you haven't restored one fails., **Hygiene ships through gates features** maintenance bypass around quinn checklist., **Baselines dated measurements, memories** two skills depend them true., **Expiries calendar entries, surprises** owner lead time dated credential., **Skipped runs logged skips** visible debt, never silence., **Thresholds operator-adopted; ops proposes labeled reasoning** (rules 0.5/0.6). · f=monitoring connector bound baselines whatever measurable (logs, synthetic checks quinn's browser machinery), loudly labeled partial; connector adoption surfaced operator (plan §5: Datadog candidate)., backup infrastructure exists finding #1, severity high, proposed operator before any hygiene work everything else assumes recoverability., Cadences unset run once establish register's initial state, propose cadences what found (labeled reasoning-based), schedule nothing silently. · b:with Other Skills=**release-discipline** (sibling) consumes baselines deploy verification ships hygiene's updates; measured restore time feeds rollback reality., **incident-response** (sibling) escalates failed restore tests (P2) unwatched-expiry incidents; post-mortems send baseline/alert gaps here., **platform-playbooks** (sibling) holds tool-specific mechanics (which backup service, which monitoring product, stack-profile)., **quinn**: hygiene changes pass gate; flaky/coverage registers quinn's register infrastructure-side, deliberately parallel discipline., **dev/stack-profile**: dependency inventory's source truth; drift found dev's finding rule on., **aegis** (when built): CVE triage above patch-level severity becomes joint work; until then, most-restrictive reading operator escalation.