f=Engineering/ops/operational/tool/ops-tool-requirements.md · p=What ops needs operate full capability, what happens without each. external tool call ops makes plan-locked (Rail sandboxed, egress-allowlisted (Rail deploys included. · h2:Requirements=\| Need \| Tool / access \| Used by \| Without it \| · h2:Requirements=\|---\|---\|---\|---\| · h2:Requirements=\| CI/CD pipeline control (build, deploy, rollback triggers) \| per stack-profile; **Harness.io** MCP candidate (plan §5) \| release-discipline \| Manual deploys per playbook mechanics, checklist still mandatory, degraded loudly \| · h2:Requirements=\| Telemetry / alerting (baselines, incident detection) \| per stack-profile; **Datadog** MCP candidate (plan §5) \| maintenance-hygiene, incident-response \| Human-report detection (logged as a contributing factor every incident); baselines from logs/synthetic checks, labeled partial \| · h2:Requirements=\| Hosting/platform control plane \| per stack-profile (host CLI/API via playbook) \| release-discipline, platform-playbooks \| Method-only guidance; operator executes manually per playbook \| · h2:Requirements=\| Backup service access (trigger restore tests to scratch envs) \| per stack-profile \| maintenance-hygiene \| Restore tests become operator-run tasks on cadence; unverified backups flagged as such \| · h2:Requirements=\| Dependency/CVE scanning \| per stack-profile CI or a scanner connector \| maintenance-hygiene \| Manual advisory review on cadence, labeled partial; joint triage with aegis when built \| · h2:Requirements=\| Append-only record access \| deploy records \· incident log \· maintenance register (config paths) \| all \| Affected skill stops issuing completion claims; loud degradation \| · h2:Requirements=\| Staging / scratch environments \| per stack-profile \| release-discipline (rollback exercise), maintenance-hygiene (restore tests) \| Production-shaped ephemeral envs, labeled; the gap itself is infrastructure debt, flagged \| · h2:Explicit non-needs (by design)=**No destructive database access even mid-incident.** Data changes dana-authored prepared scripts OPERATOR runs (Rail 3). ops sequences them; never executes them., **No application-code write access** findings route owning builder; ops ships observes, doesn't patch code., **No egress beyond allowlist** deploy tooling's network needs named playbook's Charter notes operator-approved (Rail 2). · n=Connector adoption surfaced operator deployment (plan pattern); skill degrades method-only fallback section., New tooling enters playbook update file config under locked plan, never hoc.