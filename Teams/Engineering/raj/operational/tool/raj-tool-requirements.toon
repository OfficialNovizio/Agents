f=Engineering/raj/operational/tool/raj-tool-requirements.md · p=What raj needs, what happens without each. external tool call plan-locked (Rail sandboxed (Rail 2); backend runs destructive data changes (Rail 3). · h2:Requirements=\| Need \| Tool / access \| Used by \| Without it \| · h2:Requirements=\|---\|---\|---\|---\| · h2:Requirements=\| Repo read/write for backend code \| repo scope (per stack-profile) \| all skills \| Core; without write, design-only \| · h2:Requirements=\| Backend framework + runtime \| per stack-profile \| service-patterns, api-standards \| Method-only guidance \| · h2:Requirements=\| Datastore READ access \| per dana's `read_access_scope` (Rail 3) \| data-access-discipline \| Can't build data access; escalate \| · h2:Requirements=\| Queue/async infrastructure \| per stack-profile \| service-patterns \| Sync-only, labeled; flag async gaps \| · h2:Requirements=\| Telemetry backend \| ops connector (Datadog candidate, §5) \| backend-observability \| stdout logs + metrics endpoint, labeled \| · h2:Requirements=\| Contract-test runtime \| test framework (quinn's tiers) \| api-standards \| No gate evidence → not done \| · h2:Explicit non-needs hard prohibition (by design)=**NO destructive/schema datastore execution** reads within scope only; schema bulk changes dana's migrations OPERATOR runs (Rail 3). raj authors none., **No production deploy control** ops ships; raj's code passes quinn's gate first., **No secrets logs code** charter aegis. · n=raj primary builder, genuine code write access change passes dev's review (incl. integrity checks agent-authored code), quinn's gate, aegis risky surfaces., New infra (queue, telemetry) enters stack-profile config, under locked plan.