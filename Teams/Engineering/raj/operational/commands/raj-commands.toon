f=Engineering/raj/operational/commands/raj-commands.md · p=Routing reference raj: which phrase invokes which skill, overlapping backend vocabulary resolves. · h2:Trigger Table=\| Skill \| Natural-language triggers \| Shortcut \| · h2:Trigger Table=\|---\|---\|---\| · h2:Trigger Table=\| api-standards \| "design endpoint," "API design," "error shape," "version," "contract test" \| `/raj-api` \| · h2:Trigger Table=\| service-patterns \| "async," "idempotency," "retry/timeout," "circuit breaker," "queue," "service boundary" \| `/raj-service` \| · h2:Trigger Table=\| data-access-discipline \| "query from API," "N+1," "transaction," "connection pool" \| `/raj-data` \| · h2:Trigger Table=\| backend-observability \| "logging," "tracing," "metrics," "health check," "instrument" \| `/raj-observe` \| · h3:"slow endpoint" which layer?=DB-bound (N+1, slow query) **data-access-discipline** (then dana/db-performance)., Dependency-bound (slow downstream) **service-patterns** (timeout/breaker)., Unknown **backend-observability** first (trace it), right layer. · h3:"the API needs change data bulk alter schema"=raj. Routes **dana** (migration-discipline) operator runs (Rail 3). raj's backend never executes destructive/schema changes; reshape request migration. · h3:Observability build-time, incident-time="Add logging debug incident" logging already exist; add note done-definition gap, one-off. · h3:What raj never does=Execute schema change bulk destructive data operation (Rail dana operator)., Ship unversioned breaking change unbounded list endpoint., Log secrets or PII. · f=clear match it's contract, api-standards; behavior, service-patterns; data, data-access; visibility, observability. Any backend request implying destructive/schema data change reshaped dana migration operator (Rail 3).