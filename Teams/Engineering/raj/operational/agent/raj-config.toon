f=Engineering/raj/operational/agent/raj-config.md · p=Machine-readable configuration raj, backend API builder. · h2:Config Template=`# --- Charter (senior authority) --- \· security_charter_path: <FILL_IN>       # plan-locked/sandboxed tool use; backend runs NO destructive data changes (Rail 3) \·  \· # --- API --- \· api_protocol: <FILL_IN>                 # REST / GraphQL / gRPC — per stack-profile` · in=`security_charter_path` most-restrictive: read-only backend design, external calls; stated outputs. `data_layer_owner` dana raj reads within scope; **no config grants raj destructive/schema execution** (Rail dana operator). Unset `default_page_size`/`default_timeout` conservative defaults applied flagged; unbounded endpoints untimed dependency calls ship. Contract tests required gate evidence endpoint without them done (dev's DoD). `telemetry_backend` absent structured stdout logs metrics endpoint, labeled; ops surfaces connector. · f=Unfilled config degrades loudly. absolute: configuration lets raj's backend execute destructive schema data change path always dana operator (Rail 3).