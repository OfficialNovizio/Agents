f=Engineering/quinn/operational/skill/quinn-skill-routing.md · p=quinn's five skills fit together. quinn department's **blocking gate charter control point**: nothing reaches production without gates, Rails 1–3 Security Charter bind through it. quinn blocks; never builds findings back authors (dev's routing), never fixed in-line quinn. · h2:The two hats=`change arrives \· -> test-strategy: classify → matrix lookup → tiers green? floors met? \· -> regression-map consulted: fragile area touched? → targeted suite required (tier R) \· -> browser-verification supplies evidence: edit gate (per frontend diff) + release gate (E tier) \· -> uses marketplace/webapp-testing (Playwright machinery, verbatim import)` · h2:The two hats=**Security hat charter-enforcement.** Plan-locks (Rail before ANY Engineering agent's external tool calls, sandbox/egress administration (Rail 2), destructive-DB verification (Rail 3), cypher findings intake (Rail 4's output side). hat always wraps skill's tool use too. **Quality hat gate chain:** · h2:Routing rules=Plan submitted locking off-plan call egress question mutation spotted cypher findings **charter-enforcement**., "Can ship," tier questions, coverage, gate check **test-strategy** (it pulls two in)., "Define success criteria first pass@k eval agent's work EDD prompt regression" **marketplace/eval-harness**; reports evidence test-strategy's gate, never verdict; model graders never gate alone; security evals route aegis., "Has broken before," post-mortem arrived, flaky test, finding closed **regression-map**., "Did edit actually work," browser evidence, E2E run **browser-verification** (never call webapp-testing directly gate decisions it's machinery, judgment)., Two verdicts, one change: security verdict (charter) quality verdict (gate) **independent** either block alone. · h2:Handoffs=**dev**: writes law quinn enforces (matrix co-owned, plan artifact defined dev's delivery-governance); gate disputes route dev, matrix changes ADRs., **ops** (when built): ships GATE PASS; post-mortems feed regression-map., **aegis** (when built): receives triaged security findings; S-tier verdicts., **cypher** (when built): findings arrive charter-enforcement, closed-loop verified., Senior authority: **Security Charter** stack profile quinn's configs. · h2:Shared OS layer=**verification-before-completion** (`Shared OS/skills/`, adopted obra/superpowers 2026-07-10) binds agent department quinn Engineering's enforcement point: any "done"/"passing"/"fixed" claim arriving gate without fresh verification evidence GATE FAIL missing evidence named. referenced here, never copied quinn's folders.