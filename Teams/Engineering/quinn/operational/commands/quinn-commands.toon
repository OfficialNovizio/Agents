f=Engineering/quinn/operational/commands/quinn-commands.md · p=Routing reference quinn: which phrase invokes which skill, overlapping "gate/verify/check" vocabulary resolves. · h2:Trigger Table=\| Skill \| Natural-language triggers \| Shortcut \| · h2:Trigger Table=\|---\|---\|---\| · h2:Trigger Table=\| charter-enforcement \| "lock this plan," "plan-lock," "sandbox policy," "egress request," "charter check," "rail violation," "cypher findings" \| `/quinn-lock` \| · h2:Trigger Table=\| test-strategy \| "can this ship," "gate check," "release gate," "what tests does this need," "coverage floor" \| `/quinn-gate` \| · h2:Trigger Table=\| regression-map \| "fragile areas," "has this broken before," "add a map entry," "flaky test," post-mortem intake \| `/quinn-map` \| · h2:Trigger Table=\| browser-verification \| "verify this edit," "browser evidence," "run the E2E," "did it actually render" \| `/quinn-verify` \| · h2:Trigger Table=\| webapp-testing (marketplace) \| never invoked directly for verdicts — machinery called BY browser-verification \| — \| · h3:"check/verify which skill?=**plan tool call** charter-enforcement (security hat)., **release change** ("can ship") test-strategy (which pulls map browser evidence)., **specific edit claim** browser-verification. · h3:"check/verify which skill?=object ambiguous, ask gate guesses gate. · h3:"gate" hats, always=Any gate check runs verdicts: quality (test-strategy chain) security (charter-enforcement scan diff/plan). Either blocks alone. Reporting one verdict "the" verdict wrong construction. · h3:Findings arrive intake before fix=Incoming cypher/aegis/ops material routes charter-enforcement (intake/triage) regression-map (post-mortem entries) quinn never fixes; authors fix, quinn re-verifies. · h3:Blocking is not negotiating="Just once," "it's urgent," "the tests probably fine" answer named gap list and, genuine emergencies, matrix's hotfix row. Pressure waive floor rail routes operator (floors) refused outright (rails). · f=clear match ask rather guess. request implies quinn modify code, decline route owning builder quinn blocks verifies; build.