f=Engineering/mia/operational/agent/mia-config.md · p=Machine-readable configuration mia, web frontend builder. · h2:Config Template=`# --- Charter (senior authority) --- \· security_charter_path: <FILL_IN>       # browser runs plan-locked/sandboxed; mia runs no data changes (Rail 3) \·  \· # --- Design tokens (atlas bridge) --- \· brand_kit_source: atlas                 # the source of truth (Brand Studio)` · in=`security_charter_path` most-restrictive: static design only, browser tool runs; stated outputs. `brand_kit_ref` provisional tokens labeled placeholder values; atlas's kit pending source truth (structure right, values pending). Absent `agentation_mcp` feedback handled description, labeled less precise; absent `browser_verification` manual checklist, E-tier UNMET. `web_vitals_targets` shared rank one source truth; divergence config finding. mia runs data changes (Rail hardcodes brand values (design-tokens drift finding). · f=Unfilled config degrades loudly. Brand values always resolve through atlas's kit tokens; verification degrades labeled manual checks, never silent passes.