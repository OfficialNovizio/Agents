f=Governance/board/operational/agent/board-config.md · p=Machine-readable configuration board. per-business values skills reference never hardcode default they embody business's rules risk appetite, one operator-set. · h2:Config Template=`# --- Governing documents --- \· constitution_path: <FILL_IN>          # the business's constitution file (start from \· # custom/constitution-enforcement/assets/constitution-template.md) \· # — constitution-enforcement Phase 1 \· locked_commitments_path: <FILL_IN>    # the locked strategic commitments file (start from` · in=Until `constitution_path` `locked_commitments_path` set (and files written operator), constitution-enforcement strategic-veto stop Phase offer their templates board enforcement veto power wasn't given writing. Until three fiduciary thresholds set, fiduciary-guard stops asks never defaults them. Until `mitigation_gate` set, risk-assessment-matrix uses catalog protocol value (12) built skill. Until log/register destinations set, rulings operator directly, stated explicitly output. Add fields skill actually references them. · f=unfilled config never silently blocks silently passes anything: skill's documented fallback (stop-and-ask, deliver-to-operator) applies.