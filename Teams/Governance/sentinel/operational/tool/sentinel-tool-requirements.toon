f=Governance/sentinel/operational/tool/sentinel-tool-requirements.md · p=What sentinel's skills technically needs. Governance values live `sentinel-config.md` (and board-config shared fields). **This file specifies needs grant them.** Listing capability doesn't give sentinel capability; actual access separate runtime-configuration step wherever sentinel deployed. table checklist whoever configuration. · tr:by Skill=\| Skill \| Required \| Optional / Future \| Source line \| · tr:by Skill=\|---\|---\|---\|---\| · tr:by Skill=\| audit-trail-design \| File read (architecture/requirements inputs); file write (the design outputs) \| — \| "Reads system architecture… Does not implement logging infrastructure" \| · tr:by Skill=\| constitution-watch \| File read (constitution via board-config; output stores per `sweep_scope`); file append (sweep reports, warnings — audit events) \| Scheduled/automated execution for true continuous cadence (until then: operator-invoked) \| Phase 2 sampling; Phase 5 logging \| · tr:by Skill=\| gate-bypass-detection \| File read (board's criteria/documents; action records per `bypass_scan_scope`; the shared decision log for matching); file append (scan reports — audit events) \| Connector to live ledger/contract systems (until then: operator-supplied exports) \| Phases 1–3; Phase 6 logging \| · n=scripts Python requirement sentinel's work reading, classifying against written tests, appending reports. Pattern-matching automation (e.g., scripted sweep filters) plausible future addition, proposed tested playbook 5.2 sweep volume demands it, assumed now., Sentinel read-hungry agent far: value scales directly which stores read. access-control design reading itself specified audit-trail-design skill (least privilege, exports audited)., web search deferred shared OS-level layer. · h:to Apply=Read access configured scopes append access audit log locations floor sentinel function designed.