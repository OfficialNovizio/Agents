f=Governance/precedent/operational/agent/precedent-config.md · p=Machine-readable configuration precedent deliberately tiny: precedent mostly consumes board's config rather duplicating it. · h2:Config Template=`# --- Shared with board (single source of truth: board-config.md) --- \· decision_log_destination: <SEE board-config>   # precedent reads/appends the SAME log board \· # writes to — set it once in board-config; \· # this line is a pointer, not a second value. \· ` · in=`decision_log_destination` **board's field** never set separately here; forked log forked memory. Until `retrieval_top_n` set, ruling-log uses built-in Until `index_threshold` set, precedent raises index proposal retrieval visibly degrades, judgment call flagged operator. Add fields skill actually references them. · f=unfilled config never blocks: skills use their documented defaults deliver operator directly destinations missing.