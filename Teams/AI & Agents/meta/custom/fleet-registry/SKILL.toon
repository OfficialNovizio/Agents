f=AI & Agents/meta/custom/fleet-registry/SKILL.md · in=single source truth WHO exists fleet: agent, department, pod, leader flag, lifecycle state, skill count, dormancy switches. Built scratch. registry document lives `assets/fleet-registry.md`, seeded 2026-07-10 agents built far. · p=Without registry, "the fleet" folder tree someone re-derive session. registry answers instantly: what agents exist, which dormant why, which slots deployment platform fill, what changed when. · wu=agent created (post-board-approval), renamed, made dormant, retired., proto promotes archives prototype., Deployment needs roster→platform-registry mapping., Anyone asks "who X?" "does agent exist?" · st:/ Protocol=EVENT (board-approved change promotion dormancy trigger) APPEND entry UPDATE roster table CROSS-CHECK against folder tree (they agree; disagreement incident Fleet Charter Rail 3). · in=Registry entries **append-only**: state changes add dated line agent's history, never overwrite. Retirement state (`retired`), deletion. Lifecycle states: `prototype` (caged, Rail `active` `dormant(<switch>)` `retired`. Dormancy always names wake condition (e.g. `dormant(mobile_active)`, `dormant(operator-scope-doc)`). entry records: name, department, pod, role, leader?, skill counts (custom/marketplace/shared), state, wake condition dormant, date, board proposal authorized it. Name collisions checked HERE, proposal time before board sees proposal (relay/gauge exist because check didn't). deployment: export roster platform's agent registry format (see agent-architecture-standards' platform map); dormant agents export their switch, omitted. Reconcile registry folder tree audit cadence (`<FILL_IN: cadence, suggested quarterly>` shared relay's tool audit). · of=Registry entries table rows (see assets/fleet-registry.md); reconciliation reports PASS discrepancy list, discrepancy tagged `registry-stale` `unauthorized-change` (the latter Rail incident). · pr=registry describes reality; never authorizes (authorization board's, Rail 3)., Append-only, always history point (precedent's ledger discipline, fleet edition)., dormant agent real agent: registered, auditable, wake-condition explicit. · f=folder tree registry disagree history can't resolve which right, folder tree wins CONTENT, registry wins AUTHORIZATION unauthorized folder quarantined pending operator decision. · b:with Other Skills=agent-architecture-standards defines what registered agent look like; skill records exists., relay's mcp-tool-registry capability registry (tools); roster registry (agents). discipline, different subject., fleet-governance routes proposals whose verdicts registry records.