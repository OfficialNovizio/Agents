f=AI & Agents/relay/custom/egress-allowlist-authoring/SKILL.md · in=Exports network view tool registry **egress allowlist** Engineering Rail (sandbox tool call, egress-allowlisted) enforces runtime. relay authors; runtime enforces; quinn owns sandbox policy. Authoring enforcement split dana/Rail · p=Rail good list. allowlist maintained hand drifts registry; skill makes registry single source allowlist derived artifact. · wu=Any registry change touching egress domains (new tool, revocation, scope change)., quinn requests current allowlist sandbox policy., egress-denial incident needs "should domain allowed?" answer. · st:/ Protocol=DERIVE (registry per-tool domain list) MINIMIZE (exact hosts over wildcards; transitive "it call...") VERSION (dated export) HAND OFF (to quinn/runtime) RECONCILE (denials list). · in=allowlist always **derived** registry never edited directly. domain traceable registered tool's egress field doesn't ship. Exact hostnames over wildcards; wildcard requires operator sign-off recorded registry history. Tools egress NONE unresolved `<FILL_IN>` contribute nothing they fail closed sandbox until operator resolves them. correct behavior, bug. export dated versioned; quinn's sandbox policy names version enforces. Egress-denial incidents: denied domain registry-traceable, export stale re-derive hand off. traceable, that's possible exfiltration attempt off-plan call: escalate quinn aegis, never "just add domain." · of=Dated allowlist export: egress-allowlist vYYYY-MM-DD` one domain line source tool comment. Reconciliation verdicts: `stale-export not-traceable (escalated)`. · pr=registry upstream, always fixing allowlist means fixing registry., Fail closed feature: blocked legitimate call costs minutes; allowed exfiltration unbounded., Never widen list make incident away. · f=Runtime allowlist enforcement known deploy gap Rail Hermes-runtime work PROJECT-HANDOFF §1)? Keep authoring versioned exports anyway; list ready day enforcement lands, documents intent audits meanwhile. · b:with Other Skills=mcp-tool-registry: source data here., quinn (Engineering): consumes exports, owns sandbox policy Rail 1–2 enforcement., aegis: judges suspicious-domain escalations (its detection classes cover exfil patterns).