f=AI & Agents/relay/custom/mcp-tool-registry/SKILL.md · in=canonical record capability any agent call: MCP servers, connectors, APIs, scripts external effect. Fleet Charter Rail made registry law: **no unregistered capability, ever.** Registry document: `assets/tool-registry.md`; mechanical check: `scripts/registry_lint.py`. · p=Tool access prompt-injection excessive-agency risk becomes real damage. complete registry what makes plan-lock (Engineering Rail checkable egress allowlist (Rail authorable. · wu=Any agent wants new tool/MCP/connector BEFORE first use., scout's tool-evaluation-intake produces adopt verdict., Access questions ("who call X?"), audits, deregistrations. · st:/ Protocol=REQUEST SCREEN (security review aegis's detection-classes asset shared reference, aegis owns it) REGISTER (entry template) GRANT (least-privilege-grants skill) LINT (`registry_lint.py`) AUDIT (cadence). · in=Entry fields (all required lint enforces): tool name, kind (MCP/API/script/connector), source/URL, auth method, owner (an agent operator), scopes, per-agent access map, egress domains, date, status (`active/trial/revoked`). Append-only: revocation new `revoked` entry, never deletion. Trial status (from scout's sandbox trials) auto-expires: `<FILL_IN: trial period, suggested days>` expiry without adopt verdict revoked. Egress domains recorded tool feed egress-allowlist-authoring skill tool declared egress gets NONE. Run `python scripts/registry_lint.py assets/tool-registry.md` after change; failing registry blocks change reported complete (verification-before-completion applies). Registration records capability; never grants grants runtime/operator's (same authoring-vs-enforcement split dana/Rail 3). · of=Registry entries template rows; lint output: `OK` per-entry violations; audit reports: grants confirmed revoked-by-default list. · pr=Unregistered off-plan halt (Fleet Charter Rail 1)., registry describes; runtime enforces; operator owns., Append-only history who access always answerable. · f=Tool's egress needs unknown? Register egress NONE trial status fails closed sandbox until someone declares domains. Owner unknown? registrable; find owner first. · b:with Other Skills=scout's tool-evaluation-intake feeds registry; skill never evaluates., least-privilege-grants decides WHO gets access; skill records it., egress-allowlist-authoring exports network view registry., meta's fleet-registry agent roster; capability roster.