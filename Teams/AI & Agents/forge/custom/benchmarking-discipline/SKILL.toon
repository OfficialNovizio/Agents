f=AI & Agents/forge/custom/benchmarking-discipline/SKILL.md · in=models/techniques compared honestly: task-representative eval sets, blind scoring, cost-quality frontier reads. method makes model-technique registry's numbers trustworthy. · p=Un-disciplined benchmarks (cherry-picked tasks, scorer knows candidate, single runs) produce confident nonsense drives routing. discipline product. · wu=new model/technique candidate needs numbers (from technique-adoption edge's pilots)., degradation diagnosis needs "would alternative better?" comparison., Periodic re-benchmark incumbents (`<FILL_IN: cadence, suggested provider release cycle>`). · st:/ Protocol=SET (use operator's golden task set set gauge uses; never bespoke set benchmark) RUN (all candidates identical inputs, configs recorded) SCORE BLIND (scorer human rubric know which output came which candidate) REPLICATE `<FILL_IN: suggested 5>` runs task variance matters; single samples lie) COST (full cost task: tokens, latency, retries) FRONTIER (registry update). · in=benchmark without golden set: bespoke task set invented one benchmark cherry-picking vector golden set lacks needed task type, that's golden-set change proposal (Rail first. Blind scoring non-negotiable: outputs shuffled identity-stripped before scoring. mechanical rubric exists, mechanical blind-judgment scores recorded. Record EVERYTHING: model versions, generation configs, prompt text hashes, run dates. unreproducible benchmark anecdote. Report variance, means overlapping distributions mean "no measured difference," report says exactly that. Negative/null results enter registry too: "no improvement" finding prevents next person re-running comparison. · of=Benchmark report: setup block (reproducibility), per-task-type score tables variance, cost table, frontier delta, verdict (`adopt-candidate no-difference incumbent-wins`), confidence flag. · pr=set, blind scores, multiple runs isn't benchmark., Variance result; null result., benchmark recommends; operator decides (via Rail configs change). · f=golden task set (the current state)? Benchmarks limited coarse public-task smoke comparisons, results marked `provisional golden-set-grade`, never used alone change routing. golden set gap department's loudest pending item. · b:with Other Skills=model-technique-registry consumes result; degradation-diagnosis requests comparisons; technique-adoption supplies candidates., gauge's llm-ops runs golden set watch incumbents one set, two uses (compare monitor); set governance shared Rail 3-gated.