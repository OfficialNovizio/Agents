f=AI & Agents/forge/custom/model-technique-registry/SKILL.md · in=current inventory models techniques use agent measured cost/quality task type plus routing recommendations derived it. fleet's "which model/technique what" answer, backed benchmarks instead vibes. · p=Model choice quietly dominates cost quality. Without registry, routing decision re-litigates memory; one, cites measured frontier. · wu="Which model/technique X?" any routing question., benchmark run (benchmarking-discipline) produces new results., provider releases/changes model (version event gauge's llm-ops). · st:/ Protocol=RECORD (model/technique, version, per-task-type cost quality scores, date, benchmark ref) FRONTIER (cost-quality task type dominated options marked) RECOMMEND (routing rec task type, confidence-flagged) HAND OFF (recs Rail proposals they change agent's config; operator/platform decides model choice operator config, never skill-forced). · in=Entries dated benchmark-referenced: unmeasured entry says `unmeasured` exist registry never recommendation. Task types come operator's golden task set categories (`<FILL_IN>`); until exists, task typing coarse (generation/analysis/tooling) flagged reasoning-based. frontier recomputed new benchmark: strictly-dominated options (worse quality higher cost) get marked `dominated` kept history, excluded recs. Recommendations always state: frontier position, migration cost note (config change? prompt rework? re-golden-run?), confidence flag rule 0.6. Model-agnosticism preserved: skills never hardcode model; registry recommends, operator's platform config binds (the current runtime-vs-plan divergence noted PROJECT-HANDOFF exactly split working designed). · of=Registry table rows (append-only history like fleet registry); frontier summaries task type; routing recs short memos migration-cost confidence lines. · pr=recommendation without measurement behind it., Dominated fact, preference registry records facts flags rest., operator owns final routing decision, always. · f=benchmarks run yet? registry still records WHAT use (from meta's fleet registry version stamps) everything marked `unmeasured` honest inventory precedes informed one. · b:with Other Skills=benchmarking-discipline produces numbers; skill stores serves them., technique-adoption handles NEW candidates; registry tracks incumbents adopted., gauge's llm-ops watches incumbents production; version events sync ways., edge gates emerging TECH broadly; forge gates models/techniques specifically.