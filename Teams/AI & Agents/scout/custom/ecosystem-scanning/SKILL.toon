f=AI & Agents/scout/custom/ecosystem-scanning/SKILL.md · in=recurring sweep tool/skill ecosystem code hosts, MCP directories, skill marketplaces filtered against fleet's actual gaps, producing shortlist, firehose. · p=ecosystem moves weekly; fleet shouldn't chase it, shouldn't discover load-bearing tool year late. disciplined scan converts noise small, gap-matched shortlist. · wu=Scan cadence fires (`<FILL_IN: suggested weekly, catalog default>`)., named gap needs targeted sweep ("find X")., department build begins (pre-build marketplace pass playbook's search, standing). · st:/ Protocol=SOURCES (dated list: assets kept per-scan currently skillsmp.com, mcpmarket.com, awesomeskill.ai, github topic feeds, MCP registry `<FILL_IN: operator additions>`) SWEEP FILTER (against gap register: current `PENDING` items agent.md files, logical-book wants, integration candidates) SHORTLIST (max `<FILL_IN: suggested 5>` scan, source URL which gap matches) HAND OFF (tools tool-evaluation-intake; skills marketplace-skill-scouting). · in=filter skill: nothing enters shortlist without named fleet gap addresses. "Cool" gap. Sources dated asset dead degraded sources get replaced scan, noted scan log. scan logs: sources swept, items considered (count), shortlist (with URLs), zero-result gaps (so recurring empty searches become "build custom" evidence). Repeat candidates cross-checked against adopt-reject registry first re-surfacing logged rejection requires new information, stated. Scanning read-only reconnaissance: installs, trials, registrations belong intake skills downstream. · of=Dated scan log: sources, counts, shortlist table (item URL matched gap route), zero-result gaps. · pr=Gap-matched discarded shortlist small purpose., Zero results findings; logged rejections stay rejected without new evidence., Look, don't touch: reconnaissance never installs. · f=Sources unreachable scan tooling unavailable? Log skipped scan explicitly gap coverage fact fleet see) never backfill memory. · b:with Other Skills=tool-evaluation-intake marketplace-skill-scouting consume shortlist; adopt-reject-registry checked first updated last., radar (future Market Intelligence) scans MARKETS; scout scans TOOL/AI ecosystem clean subject split (redesign plan §5)., forge receives technique-shaped finds; edge receives platform/infrastructure-shaped finds.