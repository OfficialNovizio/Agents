f=Executive Office/echo/operational/tool/echo-tool-requirements.md · p=What echo's skills technically needs function, read directly off their instructions. Technical-requirements layer `operational/agent/echo-config.md` governance/values layer (metrics source, approval contact, send channel), what's actually needed run. **This file specifies needs grant them.** Listing "Python/shell execution: required" doesn't give echo capability. Actual tool/file/execution access separate runtime-configuration step, set wherever echo actually gets deployed Claude Skills-compatible platform's permission system, whatever infrastructure human operator sets run process manually). table checklist whoever configuration, functioning permission grant itself. · tr:by Skill=\| Skill \| Required \| Optional / Future \| Source line \| · tr:by Skill=\|---\|---\|---\|---\| · tr:by Skill=\| pitch-narrative \| File read/write (version log) \| — \| Phase 5: "save the output as a new version within this skill's working file" \| · tr:by Skill=\| pitch-framework \| File write (self-contained HTML output) \| Python + `python-pptx` for PPTX conversion (script not yet built) \| Phase 5: "Output as a single self-contained file"; Fallback flags the PPTX script as not yet built \| · tr:by Skill=\| investor-update-template \| File read/write (drafts); Python/shell execution (runs investor-update-generator's validator) \| — \| Phase 4: "Run the draft through investor-update-generator's scripts/investor_update_validator.py" \| · tr:by Skill=\| investor-update-generator (marketplace) \| Python/shell execution (`investor_update_validator.py`, tested and confirmed working); file read (reads the draft update) \| — \| "Quick Start": `python scripts/investor_update_validator.py update.md` \| · n=skill requires web search marcus, stays deferred shared OS-level layer, added locally., one real "optional/future" item pitch-framework's PPTX-conversion script, which explicitly flagged built during skill's construction it's ever built, row updated "optional" "required specific fallback path.", Echo's Python dependency narrower looks: investor-update-generator's validator actually needs it, investor-update-template inherits need depending investor-update-generator it's one real script, referenced two places. · h:to Apply=filling tool permissions (if echo ever gets config field doesn't currently, see `echo-config.md`'s 3-field scope), table floor: file read/write Python/shell execution permitted minimum echo's skills run designed.