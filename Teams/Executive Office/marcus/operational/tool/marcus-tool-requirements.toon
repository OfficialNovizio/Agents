f=Executive Office/marcus/operational/tool/marcus-tool-requirements.md · p=What marcus's skills technically needs function, read directly off their instructions. technical-requirements layer; `operational/agent/marcus-config.md`'s `tool_permissions` field governance layer (what's *allowed*) file what's actually *needed*, whoever fills config see gap between two. **This file specifies needs grant them.** Listing "Python/shell execution" doesn't give marcus capability. Actual tool/file/execution access separate runtime-configuration step, set wherever marcus actually gets deployed Claude Skills-compatible platform's permission system, whatever infrastructure human operator sets run process manually). table checklist whoever configuration, functioning permission grant itself. · tr:by Skill=\| Skill \| Required \| Optional \| Source line \| · tr:by Skill=\|---\|---\|---\|---\| · tr:by Skill=\| decision-critic \| None (pure reasoning) \| Access to a second model (Codex, Gemini, another Claude instance) for cross-checking \| "If the operator wants a second opinion and another model is reachable, run the same plan through it" \| · tr:by Skill=\| okr-cascade \| File write (save markdown output) \| File read (if context is supplied as documents) \| "Save substantial output as a markdown document... if requested" \| · tr:by Skill=\| venture-priority-matrix \| Python/shell execution (runs `scripts/priority_matrix.py`); file read/write (JSON input, optional JSON output) \| — \| Explicit script usage in Instructions phase 4; has a manual-reasoning fallback if Python isn't available \| · tr:by Skill=\| strategy-advisor \| None (pure reasoning framework) \| — \| Verbatim marketplace copy — no tool instructions in the source \| · tr:by Skill=\| vision-exploration \| File write (archive end-state artifacts to a directory) \| — \| "Archive files under a topic-named directory... Confirm the directory and file names with the user" \| · n=decision-critic venture-priority-matrix any *optional* tool need beyond standard reasoning/file access everything else either pure reasoning (strategy-advisor) basic file I/O (okr-cascade, vision-exploration)., skill currently requires web search. future revision any skill adds research/benchmarking steps (e.g. reintroducing something like brainstorm-okrs's original "use web search understand industry benchmarks" step, which carried final okr-cascade merge), file updated reflect time., venture-priority-matrix's Python requirement hard dependency external runtime among marcus's skills everything else degrades gracefully reasoning-only. · h:to Apply=filling `operational/agent/marcus-config.md`'s `tool_permissions.allowed` list, table floor minimum, file read/write Python/shell execution permitted marcus run skills designed. Anything beyond (web search, cross-model access, external connectors) optional deliberate policy choice, default.