f=Brand Studio/pulse/operational/agent/pulse-config.md · p=Machine-readable configuration pulse; toongine binds business. multi-platform binding point discussed 2026-07-07. · h2:Config Template=`# --- Platforms (one entry per connected platform, per brand) --- \· brands: \· - brand_id: <FILL_IN> \· platforms: \· - platform: <FILL_IN>              # e.g., instagram / tiktok / youtube / linkedin / x` · in=Unconnected platforms never planned assumption; per-run operator naming flags config gap. `access` governs capability honestly: publish access packages; engage access sweep exports. `reply_mode: auto` still respects AMBER/RED absolutely mode governs GREEN. `reply_scope_notes` extend GREEN; AMBER/RED classes floor rules cannot configured away. Add fields skill references them. · f=Unfilled config degrades loudly, never silently.