f=Brand Studio/spark/operational/agent/spark-config.md · p=Machine-readable configuration for spark. · h2:Config Template=`# --- Consumed from the definers (single sources of truth — never duplicated here) --- \· # brand kits + separation matrix → atlas-config \· voice guides → lena-config \· \· # arcs + chapter registries → weave-config \·  \· # --- Spark's own fields ---` · in=Definers' documents missing gate's power narrows recommend-only lane, stated verdict; fix definer's creation loop, spark config entry. Logs unset verdicts/overrides operator directly, stated output. Add fields skill references them. · f=Unfilled config degrades loudly, never silently.