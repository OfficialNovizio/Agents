f=Brand Studio/pixel/operational/agent/pixel-config.md · p=Machine-readable configuration for pixel. · h2:Config Template=`# --- Style (image-style-guide; one per brand) --- \· brands: \· - brand_id: <FILL_IN> \· image_style_path: <FILL_IN>     # the approved image-style file (from the template + test loop) \· ` · in=`image_style_path` provisional generic-craft runs only, labeled; setup loop proposed. `naming_convention` pixel proposes one suggestion shape asks; nothing delivers unnamed. `generation_connector` shot lists finished prompts deliverable, stated run. Add fields skill references them. · f=Unfilled config degrades loudly, never silently.