f=Product/metric/custom/product-metrics-spec/SKILL.md · in=single source metric truth product: event taxonomy versioned definitions activation, retention, engagement, north-star metric. Everyone who says "activation" means file's current version, they're wrong. · p=Two dashboards two "activation" definitions produce two confident, contradictory decisions. One versioned spec makes number traceable definition definition change visible. · wu=product needs metric truth established extended (new events, new definitions)., Anyone cites metric PRD, report, experiment (the definition reference point)., definition needs changing metrics-governance path). · st:/ Protocol=TAXONOMY (per product `<FILL_IN: product profile>`: events named `object_action`, properties, trigger moment, owner surface web/app/api) DEFINITIONS (activation, retention windows, engagement, NSM precise computable statement over taxonomy events, versioned `vN` date) PUBLISH (the spec readable file; consumers cite `metric:activation@v3`) INTERFACE (definitions export stated format data layer kai's dashboards binding Data Analytics dept's pipelines happens dept exists; interface file's export, stable regardless). · in=definition computable named events "users who love product" definition; "≥N `<FILL_IN>` core actions within days signup" is. Versioning mandatory: changed definitions get new versions; old versions stay readable (trend continuity depends knowing which version historical number used). Consumers cite versions: PRD's success metric, loom's experiment measures, gauge-style reads pin `@vN`. Unpinned citations bounce. taxonomy resists sprawl: new events need consumer named proposal time ("who reads this?") write-only events clutter storage costs. Definition changes never applied directly they route through metrics-governance (proposal, impact, version bump). · of=per-product spec file: taxonomy table versioned definitions block export interface stanza. Citation format: `metric:<name>@vN`. · pr=Computable isn't definition; versioned isn't citable., One truth product; consumers pin versions., Events earn their place read. · f=Pre-instrumentation product (no events flowing yet)? spec still written FIRST definitions drive instrumentation, reverse. Reads `MISSING` until pipes exist (gauge's honest-missing pattern). · b:with Other Skills=metrics-governance owns changes; funnel-instrumentation experiment-instrumentation implement against spec., kai (Brand Studio) owns campaign/brand analytics attribution kai's, in-product behavior metric's; shared reads cite owner's number., future Data Analytics dept binds export interface; until it's versioned file.