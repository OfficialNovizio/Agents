f=Product/metric/custom/metrics-governance/SKILL.md · in=change control metric definitions: definition never changes silently. change versioned proposal impact statement (what historical trends break, who's citing old version), sync interface data layer kai's dashboards stated export, scramble. · p=silently-redefined "activation" makes dashboard, PRD, past decision quietly wrong nobody notices until two numbers match don't. Governance makes definition changes visible, versioned, impact-assessed, keeps downstream consumer reading one export. · wu=definition needs changing (product-metrics-spec routes changes here)., new consumer (dashboard, dept, future Data Analytics layer) needs definition export., audit: live citations pinned versions still exist? · st:/ Protocol=PROPOSE (the change: old `vN` new `vN+1`, exact definition diff, reason) IMPACT (what trends break changeover, which PRDs/experiments/dashboards cite `vN`, whether backfill possible) VERSION (new version added; old version stays readable never edited away, historical numbers stay interpretable) SYNC INTERFACE (definitions export stated format; data layer river binding deferred dept, interface stable regardless) FLEET NOTE definition change skill-adjacent change shared truth material changes route through anneal board Fleet Charter Rail routine version bumps logged, board-gated). · in=silent changes definition edit proposal version bump impact statement; applying change without one truth incident (the metric-side analogue silent repricing). Old versions immortal: superseded definitions stay readable two-year-old number understood terms (append-only, precedent's discipline). Impact names names: proposal lists current citation old version (PRDs, experiments, dashboards) consumers re-pin deliberately. One export, many readers: kai's dashboards, spec's PRDs, future data layer read exported definitions divergent copies bug prevents. Material routine: changed NSM activation definition (decisions hang it) routes anneal board; clarifying non-semantic tweak logged. unsure, route (Fleet Charter most-restrictive default). · of=Change proposal: diff impact (broken trends, cited-by list, backfill?) new version export stanza board-route flag (material) log entry (routine). · pr=Versioned, impact-assessed, never silent three rules definition change., Old versions immortal; trends stay interpretable., One export, one truth, many readers. · f=data layer sync yet? export versioned file consumers read directly (the deferred-binding pattern) governance works without pipeline, pipeline binds later. · b:with Other Skills=product-metrics-spec holds definitions; owns their change; funnel/experiment instrumentation consume current versions., kai (Brand Studio): shared dashboards read export; metric owns in-product definitions, kai owns marketing ones neither redefines other's silently., anneal/board (Fleet Charter Rail 3): material definition changes route through fleet's change path; future Data Analytics dept binds export interface.