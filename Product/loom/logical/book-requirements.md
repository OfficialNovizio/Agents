# loom — Logical Layer (placeholder, rule 0.6)

Judgments flagged **reasoning-based, not formula-verified** until real sources land.

## Wants
1. **The shared statistics / experimentation text** (fleet-wide want — metric/gauge/forge/vista/ux) — grounds experiment power, minimum detectable effect, significance, and the assumption-score axes. proto's experimentation-methodology want is satisfied by the SAME acquisition — buy once, cite fleet-wide.
2. **A PMF / retention text** (shared with metric) — grounds the Ellis-bar interpretation, retention-flatness judgment, and LTV math beyond the 40% convention.

## Currently flagged reasoning-based
- ellis_disappointment_bar (40% is Ellis's convention, not a proof), retention_flatness_criterion (config).
- assumption impact × uncertainty scoring (assumption-mapping).
- min_detectable_effect / power defaults (experiment-discipline, via metric).

## Extraction protocol
When the stats + PMF sources land: power/significance/effect-size and PMF-threshold reasoning extracted with page cites; experiment-discipline + pmf-scorecard updated to cite them; rule-0.6 flags removed where a citation replaces them. Coordinate the stats buy with metric, ux, gauge, forge, proto (shared want — buy once).
