# ux — Logical Layer (placeholder, rule 0.6)

Judgments flagged **reasoning-based, not formula-verified** until real sources land.

## Wants
1. **A UX-research-methods text** — grounds study-design (method-to-question matching, sample sizing beyond the ~5/round convention, protocol/bias design) and synthesis-discipline's confidence calibration.
2. **The shared statistics text** (fleet-wide want — see metric/loom/gauge) — grounds survey significance and sample-size math for magnitude studies. Buy once.
3. **Behavioral Science dependency** (future dept): bias/persuasion theory (kahneman/cialdini et al.) is that dept's; ux applies anti-bias *method* here and flags the theory dependency rather than inlining pop psychology.

## Currently flagged reasoning-based
- default_usability_sample (~5/round is convention).
- synthesis confidence bands (strong/moderate/directional — the cutoffs).
- finding_staleness_horizon.

## Extraction protocol
When the research-methods + stats sources land: sampling/significance/method-selection rules extracted with page cites; study-design + synthesis-discipline updated to cite them; flags removed where a citation replaces them. Coordinate the stats buy with metric/loom (shared want).
