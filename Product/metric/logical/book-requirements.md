# metric — Logical Layer (placeholder, rule 0.6)

Judgments are flagged **reasoning-based, not formula-verified** until real sources land.

## Wants
1. **A statistics / experimentation text** — the SAME shared book gauge/forge/vista/sentinel/nate/kai/rio/loom want (survey significance, cohort math, experiment power, minimum detectable effect). Buy once, cite fleet-wide. Grounds: conversion-drop flag thresholds, power/sample calcs in experiment-instrumentation.
2. **A retention / LTV math source** (shared with loom) — grounds retention-window definitions and curve-flatness reads.

## Currently flagged reasoning-based
- conversion_drop_flag_threshold, power_significance_defaults (config).
- cohort-read cadence (weekly is convention).
- retention-window choices in product-metrics-spec definitions.

## Extraction protocol
When the stats book lands: significance/power/effect-size formulas extracted with page cites into this folder; experiment-instrumentation and funnel-instrumentation updated to cite them; flags removed where a citation replaces them. Coordinate the buy with loom, gauge, forge, vista (shared want).
